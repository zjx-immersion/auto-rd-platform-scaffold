# 三级导航快速实施指南

**版本**: v1.0  
**创建日期**: 2026-01-28  
**适用模块**: C2-C7 所有未实现的模块

---

## 📋 待实施模块清单

- [ ] C2 - 方案设计 (`designs/DesignListPage.tsx`)
- [ ] C4 - 迭代执行 (`sprints/SprintListPage.tsx`)
- [ ] C5 - 测试验收 (`quality/QualityListPage.tsx`)
- [ ] C6 - DevOps (`devops/DevOpsListPage.tsx`)
- [ ] C7 - 分析治理 (`analytics/AnalyticsListPage.tsx`)
- [ ] （可选）C5备选 - 发布管理 (`releases/ReleaseListPage.tsx`)

---

## 🚀 快速实施步骤

### Step 1: 导入必要的依赖

在每个 `*ListPage.tsx` 文件顶部添加：

```typescript
import { useNavigation } from '@/context/NavigationContext'
import EmptyPlaceholder from '@/components/common/EmptyPlaceholder'
```

### Step 2: 添加三级导航状态

在组件内部添加：

```typescript
export const ModuleListPage: React.FC = () => {
  const { selectedTertiaryTab } = useNavigation()
  
  // ... 其他状态
  
  // ⚠️ 注意：所有 Hooks 必须在条件返回之前调用
}
```

### Step 3: 添加数据筛选逻辑

使用 `useMemo` 根据三级导航筛选数据：

```typescript
const filteredData = useMemo(() => {
  let data = mockData
  
  switch (selectedTertiaryTab) {
    case 'all':
      return data
    case 'my':
      return data.filter(item => item.owner === '当前用户')
    case 'active':
      return data.filter(item => item.status === 'active')
    default:
      return data
  }
}, [selectedTertiaryTab])
```

### Step 4: 添加空状态处理

对于未实现的Tab，显示占位符：

```typescript
// 在所有 Hooks 之后，render 之前
if (selectedTertiaryTab && selectedTertiaryTab !== 'all') {
  const tabTitles: Record<string, string> = {
    'my': '我的数据',
    'active': '进行中',
    'archived': '已归档',
  }
  
  return (
    <div className="module-list-page">
      <EmptyPlaceholder 
        title={tabTitles[selectedTertiaryTab] || '功能开发中'}
        description="还在加速研发中..."
      />
    </div>
  )
}
```

### Step 5: 更新渲染逻辑

使用筛选后的数据：

```typescript
return (
  <div className="module-list-page">
    <Table 
      dataSource={filteredData}  // 使用筛选后的数据
      // ... 其他配置
    />
  </div>
)
```

---

## 📝 完整模板

```typescript
/**
 * ModuleListPage - 模块列表页
 */

import React, { useState, useMemo } from 'react'
import { Table, Button, Input } from 'antd'
import { useNavigate } from 'react-router-dom'
import { useNavigation } from '@/context/NavigationContext'
import EmptyPlaceholder from '@/components/common/EmptyPlaceholder'
import './ModuleListPage.css'

// Mock数据类型
interface DataItem {
  id: string
  name: string
  owner: string
  status: string
}

// Mock数据
const mockData: DataItem[] = [
  // ... 数据
]

export const ModuleListPage: React.FC = () => {
  const navigate = useNavigate()
  const { selectedTertiaryTab } = useNavigation()
  const [searchText, setSearchText] = useState('')
  
  // ⚠️ 所有 Hooks 必须在条件返回之前
  const filteredData = useMemo(() => {
    let data = mockData
    
    // 根据三级导航筛选
    switch (selectedTertiaryTab) {
      case 'all':
        break
      case 'my':
        data = data.filter(item => item.owner === '当前用户')
        break
      case 'active':
        data = data.filter(item => item.status === 'active')
        break
      case 'archived':
        data = data.filter(item => item.status === 'archived')
        break
      default:
        break
    }
    
    // 搜索筛选
    if (searchText) {
      data = data.filter(item => 
        item.name.toLowerCase().includes(searchText.toLowerCase())
      )
    }
    
    return data
  }, [selectedTertiaryTab, searchText])
  
  // 空状态处理（针对未实现的Tab）
  if (selectedTertiaryTab && selectedTertiaryTab !== 'all' && filteredData.length === 0) {
    const tabTitles: Record<string, string> = {
      'my': '我的数据',
      'active': '进行中',
      'archived': '已归档',
    }
    
    return (
      <div className="module-list-page">
        <EmptyPlaceholder 
          title={tabTitles[selectedTertiaryTab] || '功能开发中'}
          description="还在加速研发中..."
        />
      </div>
    )
  }
  
  // 表格列配置
  const columns = [
    // ... 列定义
  ]
  
  return (
    <div className="module-list-page">
      {/* 工具栏 */}
      <div className="toolbar">
        <Input.Search
          placeholder="搜索..."
          value={searchText}
          onChange={(e) => setSearchText(e.target.value)}
        />
      </div>
      
      {/* 数据表格 */}
      <Table 
        dataSource={filteredData}
        columns={columns}
        rowKey="id"
      />
    </div>
  )
}

export default ModuleListPage
```

---

## ⚠️ 关键注意事项

### 1. Hooks 顺序规则

```typescript
// ❌ 错误：Hook 在条件返回之后
if (condition) {
  return <Empty />
}
const data = useMemo(() => {...}, [])  // 错误！

// ✅ 正确：所有 Hooks 在条件返回之前
const data = useMemo(() => {...}, [])
if (condition) {
  return <Empty />
}
```

### 2. 默认Tab处理

```typescript
// 对于没有明确selectedTertiaryTab的情况，默认显示"全部"
const shouldShowAll = !selectedTertiaryTab || selectedTertiaryTab === 'all'
```

### 3. 空状态vs空数据

- **空状态**: Tab未实现，显示"功能开发中"占位符
- **空数据**: Tab已实现但数据为空，显示"暂无数据"提示

```typescript
// 未实现的Tab - 显示占位符
if (selectedTertiaryTab === 'archived') {
  return <EmptyPlaceholder title="已归档" description="功能开发中..." />
}

// 已实现但无数据 - 使用Table的empty
<Table 
  dataSource={filteredData}
  locale={{ emptyText: '暂无数据' }}
/>
```

---

## 🎯 实施优先级

### 优先级 1: 基础功能
- 实现"全部"Tab的数据展示
- 添加 `selectedTertiaryTab` 监听

### 优先级 2: 常用Tab
- 实现"我的"Tab筛选
- 实现"进行中"Tab筛选

### 优先级 3: 完善功能
- 实现其他Tab筛选
- 优化空状态提示
- 添加加载状态

---

## 📚 参考实现

| 模块 | 文件路径 | 实施状态 |
|------|---------|---------|
| 工作台 | `workspace/WorkspaceListPage.tsx` | ✅ 已完成 |
| 项目管理 | `projects/ProjectListPage.tsx` | ✅ 已完成 |
| 产品需求 | `requirements/RequirementListPage.tsx` | ✅ 已完成 |
| 规划协调 | `planning/PlanningListPage.tsx` | ✅ 已完成 |

可以参考这些已完成的模块作为实施模板。

---

## 🔍 验证检查清单

实施完成后，进行以下检查：

- [ ] 点击不同的三级导航，右侧内容会切换
- [ ] 未实现的Tab显示友好的占位符
- [ ] 没有 React Hooks 相关的警告
- [ ] 搜索功能正常工作
- [ ] 分页功能正常工作

---

**相关文档**:
- [三级导航体系设计规范](./三级导航体系设计规范.md)
- [主页面与导航大纲设计规范](./主页面与导航大纲设计规范.md)
